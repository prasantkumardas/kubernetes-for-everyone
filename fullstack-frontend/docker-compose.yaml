version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: spendwise-app:latest
    container_name: spendwise-app
    ports:
      - "5173:80"
    restart: unless-stopped
